# Docker Resources

This directory is intended for hosting individual Dockerfiles and configuration files for staging, testing, and production environments.

The root [docker-compose.yml](file:///c:/Users/devtexfina/Desktop/csalirrosasc/codeted/docker-compose.yml) is used to spin up the local development stack.
